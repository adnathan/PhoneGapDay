<!--
    Note: If you update this markup, you must recompile it on Windows 
    and replace the corresponding .xbf file under www/xbf.
-->
<Canvas xmlns:if="using:if"
        xmlns:ios="using:ios"
        xmlns:android="using:android.widget">
    <if:Android>
        <android:RatingBar StepSize="1" />
    </if:Android>
    <if:iOS>
        <ios:UISegmentedControl />
    </if:iOS>
</Canvas>
